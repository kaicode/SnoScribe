# OpenAI

```properties
llm.provider=openai
llm.openai.api-key=sk-...
llm.openai.model=gpt-4o
```

Note text is sent to OpenAI. For Docker: [../run/docker-cloud-llm.md](../run/docker-cloud-llm.md) with `LLM_PROVIDER=openai` and `LLM_OPENAI_*` in `.env`.
