# Google Gemini

```properties
llm.provider=google
llm.google.api-key=...
llm.google.model=gemini-1.5-pro
```

Note text is sent to Google. Docker: [../run/docker-cloud-llm.md](../run/docker-cloud-llm.md) with `LLM_GOOGLE_*` in `.env`.
